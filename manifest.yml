---
applications:
  .:
    name: hbbnext-idm-dev
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    url: ${name}.${target-base}
    mem: 256M
    instances: 1
    services:
      mysql-hbbnext-idm-dev:
        type: mysql
