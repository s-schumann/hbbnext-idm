<h2>Active Users</h2>

<%= render :partial => '/contexts/activeusers', :locals => { :context => @context } %>

<div class="form-actions">
	<%= link_to 'Back', contexts_path, :class => 'btn' %>
</div>
