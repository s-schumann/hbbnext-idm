<% user.devices.each do |device| %>
	<%= device.display_name %>
	<% user.udrs.each do |udr| %>
		<% if udr.device_id == device.id %>
    		(<%= user.roles.find(udr.role_id).name %>)
      	<% end %>
    <% end %>
<% end %>